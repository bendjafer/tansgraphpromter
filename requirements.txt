# ── Already provided by Lightning AI base image (do NOT re-pin) ──────────────
# torch==2.8.0+cu128   (pre-installed)
# torchvision          (pre-installed)
# networkx             (pre-installed)
# pandas               (pre-installed)
# scikit-learn         (pre-installed)
# tqdm                 (pre-installed)

# ── Hugging Face stack ────────────────────────────────────────────────────────
# Keep these three in sync — older peft/accelerate break with newer transformers
transformers==4.44.2
peft==0.12.0
accelerate==0.34.2

# bitsandbytes NOT needed — Vicuna-7B loads in fp16 (no 4-bit quantisation used)
# bitsandbytes==0.43.3

# ── Graph / PyG ───────────────────────────────────────────────────────────────
# Install torch-geometric first (pure Python), then the CUDA-compiled extensions
# via the PyG wheel index (separate pip install command — see INSTALL.md note).
torch-geometric==2.6.1

# torch-scatter and torch-sparse must be installed with:
#   pip install torch-scatter torch-sparse \
#     -f https://data.pyg.org/whl/torch-2.8.0+cu128.html
# They are NOT listed here because pip will ignore the -f flag from requirements.txt.

# OGB datasets (ogbn-arxiv, ogbn-products)
ogb==1.3.6

# ── Text embeddings ───────────────────────────────────────────────────────────
sentence-transformers==3.0.1
sentencepiece==0.2.0

# ── Experiment tracking & utilities ──────────────────────────────────────────
wandb==0.17.9
openai==1.40.0
python-dotenv==1.0.1

# timm is used in train_ddp.py (optim_factory). Only needed for DDP training.
timm==1.0.9

# TensorBoard — used in train_ddp.py
tensorboard==2.17.1
